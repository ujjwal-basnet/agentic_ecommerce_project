# Infrastructure

Render is the primary deployment target right now. Keep the root `render.yaml`
for Render Blueprint auto-detection; this folder is for deploy docs and future
Docker/Kubernetes assets.

