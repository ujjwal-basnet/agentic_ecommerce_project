"""Specialist agents — direct REST API agents that bypass MCP input/output.

These agents are triggered by UI buttons (e.g. "Try On", "Post to Facebook")
and communicate via simple JSON request/response, not MCP messages.
"""
